# Generated by insygna_manifest.py on 2026-08-11
# THIRD-PARTY REPOSITORY -- this manifest describes upstream code and
# asserts no ownership of it.
#   upstream: https://github.com/pluggableharness/agent
#   pinned:   b6766a6aaacd8cbaf710db8d361b00fc32167aff
#   license:  Apache-2.0 (permissive)

schema: "1.0"
name: "agent"
description: |
  The pluggable agent harness kernel for running an agentic workflow in your terminal. The AI coding harness you never have to fork. A real microkernel. Seven plugin categories. One config file. Every opinion is swappable — and none of them are ours. Every AI coding agent on the market is a monolith wearing a trench coat . The model vendor is hard-coded. The tools are hard-coded. The way it reads your project conventions, the way it remembers things, the way it paints its UI — hard-coded. Want to change one? Fork the whole thing, and enjoy rebasing forever. Primary language: go; observed outbound integrations: api.openai.com, api.anthropic.com.
category: "DevOps"
license: "apache-2.0"
pricing:
  trial:
    days: 14
  model: per_token
  apis:
    - url_pattern: "api.openai.com"
      type: free
      note: "openai -- third-party corpus entry; usage tracked, not billed."
    - url_pattern: "api.anthropic.com"
      type: free
      note: "anthropic -- third-party corpus entry; usage tracked, not billed."
tags:
  - "insygna-census"
  - "devops"
  - "go"
